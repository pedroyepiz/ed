const cards=document.querySelector('#cards');
function render(rows){cards.innerHTML=rows.filter(x=>x.visible).map(x=>`<article class="card"><div class="topline"><strong>ACTIVIDAD ${String(x.id).padStart(2,'0')}</strong><span class="badge">${x.actividad?'Disponible':'Próximamente'}</span></div><h2>${esc(x.titulo)}</h2><p>${esc(x.tema)}</p><div class="buttons">${x.actividad?`<a class="btn" href="detalle.html?id=${x.id}">Ver actividad</a>`:'<button class="btn" disabled>Actividad bloqueada</button>'}${x.codigo?`<a class="btn blue" href="detalle.html?id=${x.id}#codigo">Ver código</a>`:'<button class="btn blue" disabled>Código bloqueado</button>'}</div></article>`).join('')||'<p>No hay actividades visibles por ahora.</p>'}
publicData().then(render).catch(e=>document.querySelector('#message').textContent=e.message+' Abre el sitio con Live Server.');
