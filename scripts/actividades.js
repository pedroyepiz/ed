const cards=document.querySelector('#cards');
function render(rows){cards.innerHTML=rows.filter(x=>x.visible).map(x=>`<article class="ed-act-card"><div class="ed-topline"><strong>ACTIVIDAD ${String(x.id).padStart(2,'0')}</strong><span class="ed-badge">${x.actividad?'Disponible':'Próximamente'}</span></div><h3>${esc(x.titulo)}</h3><p>${esc(x.tema)}</p><div class="ed-actions">${x.actividad?`<a class="ed-button" href="detalle.html?id=${x.id}">Ver actividad</a>`:'<button class="ed-button" disabled>Actividad bloqueada</button>'}${x.codigo?`<a class="ed-button ed-blue" href="detalle.html?id=${x.id}#codigo">Ver código</a>`:'<button class="ed-button ed-blue" disabled>Código bloqueado</button>'}</div></article>`).join('')||'<p>No hay actividades visibles por ahora.</p>'}
publicData().then(render).catch(e=>document.querySelector('#message').textContent=e.message+' Abre el sitio con Live Server.');
