const id=Number(new URLSearchParams(location.search).get('id'));
const box=document.querySelector('#detail');
publicData().then(rows=>{const x=rows.find(r=>r.id===id);if(!x||!x.visible||(!x.actividad&&!x.codigo)){box.innerHTML='<h1>Contenido todavía no disponible</h1><p>Regresa a la lista de actividades.</p>';return}box.innerHTML=`<h1>Actividad ${String(x.id).padStart(2,'0')}: ${esc(x.titulo)}</h1><p>${esc(x.tema)}</p>${x.actividad?`<section class="content-block"><h2>🎯 Objetivo</h2><p>Aplicar los conceptos de ${esc(x.tema.toLowerCase())} mediante un programa o ejercicio práctico.</p><h2>📋 Instrucciones</h2><ol><li>Analiza el problema y define las operaciones necesarias.</li><li>Diseña la estructura y escribe el programa.</li><li>Prueba al menos tres casos y explica tus resultados.</li></ol><p><strong>Profesor:</strong> reemplaza este enunciado de ejemplo por las instrucciones definitivas de la actividad.</p></section>`:''}${x.codigo?`<section class="content-block" id="codigo"><h2>💻 Código de apoyo</h2><p>Reemplaza este bloque por el código de apoyo o resuelto para la actividad ${x.id}.</p><pre><code>// Actividad ${x.id}: ${esc(x.titulo)}\n// Agrega aquí tu ejemplo en C o C++.</code></pre></section>`:''}`;if(location.hash==='#codigo'&&x.codigo)document.querySelector('#codigo').scrollIntoView()}).catch(e=>box.textContent=e.message);
